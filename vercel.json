{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "export DIRECT_URL=\"${DIRECT_URL:-${DATABASE_URL_UNPOOLED:-${POSTGRES_URL_NON_POOLING:-}}}\" && prisma generate && prisma migrate deploy && next build",
  "crons": [
    {
      "path": "/api/jobs/tick",
      "schedule": "30 3 * * *"
    }
  ]
}
