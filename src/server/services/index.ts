/**
 * Business logic — one file per domain. The ONLY place data mutations and derivations live.
 *
 * Phase 0 placeholder barrel. Implementation lands in later phases —
 * see docs/REQUIREMENTS_INDEX.md and CLAUDE.md "Phase status".
 * All business logic must live under src/server; route handlers and
 * server actions stay thin (validate → authorise → call service → return).
 */
export {};
